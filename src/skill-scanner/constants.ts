/**
 * Skill 安全扫描系统常量定义
 */

// ============================================================================
// 定时任务
// ============================================================================

/** 签名扫描定时任务周期（毫秒）：60 秒 */
export const SCAN_CYCLE_INTERVAL_MS = 60_000;

/** 等待认证服务就绪的轮询间隔（毫秒）：10 秒 */
export const AUTH_WAIT_INTERVAL_MS = 10_000;

/** 等待认证服务就绪的最大时长（毫秒）：5 分钟 */
export const AUTH_WAIT_MAX_MS = 300_000;

// ============================================================================
// 签名计算
// ============================================================================

/** 签名计算时递归遍历的最大文件数 */
export const MAX_FILES_PER_SKILL = 100;

// ============================================================================
// 扫描队列
// ============================================================================

/** 扫描队列最大容量 */
export const MAX_QUEUE_SIZE = 100;

/** API 失败后最大重试次数 */
export const MAX_RETRY_COUNT = 3;

/** 队列处理完一个任务后的间隔（毫秒） */
export const QUEUE_PROCESS_INTERVAL_MS = 5_000;

// ============================================================================
// ZIP 压缩
// ============================================================================

/** ZIP 压缩文件最大体积（字节）：1MB */
export const MAX_ZIP_BYTES = 1 * 1024 * 1024;

/** Skill 原始文件总大小上限（字节）：超过此值在签名阶段即跳过，不进入压缩队列 */
export const MAX_SKILL_RAW_BYTES = 5 * 1024 * 1024;

// ============================================================================
// 强制重新扫描
// ============================================================================

/** 签名条目有效期（毫秒）：超过此时间强制重新扫描，缩小 zipSha256 被篡改的攻击窗口 */
export const FORCE_RESCAN_TTL_MS = 24 * 60 * 60 * 1000;

/** 每隔多少个扫描周期执行一次强制重扫（2 × 60s = 2 分钟），每次处理 1 个最老的过期 skill */
export const FORCE_RESCAN_EVERY_N_CYCLES = 2;

// ============================================================================
// 签名表持久化
// ============================================================================

/** 签名表持久化文件名 */
export const FINGERPRINT_STORE_FILENAME = "skill-scanner-fingerprints.json";

/** 签名表持久化格式版本号 */
export const FINGERPRINT_STORE_VERSION = 1;
